# Phase 3 WSL 2 terminal record

Date: 2026-09-17

Environment ID: `W11-U24-B52`

Status: passed

This record uses disposable homes. The host name and absolute user paths are redacted. It contains no real aliases, provider tokens, or local configuration.

## Versions

```text
WSL version: 2.7.14.0
Kernel version: 6.18.33.2-2
WSLg version: 1.0.73.2
Windows version: 10.0.26200.9457
Linux <HOST> 6.18.33.2-microsoft-standard-WSL2 x86_64 GNU/Linux
Ubuntu 24.04.5 LTS (Noble Numbat)
GNU bash, version 5.2.21(1)-release (x86_64-pc-linux-gnu)
zsh 5.9.2 (x86_64-pc-linux-gnu)
go version go1.27.1 linux/amd64
git version 2.43.0
candidate base commit: 1fa9a08bd5d3cd567d9087421fd747986471c1e0
alias-lens dev
distribution: Ubuntu-24.04
```

## Automated checks

`make fmt check` passed. A separate checklist run produced:

```text
$ GOCACHE=/tmp/alias-lens-phase3-cache go test ./...
ok   alias-lens/cmd/alias-lens
?    alias-lens/cmd/alias-lens/internal/exportfile [no test files]
ok   alias-lens/cmd/alias-lens/internal/usagelog
ok   alias-lens/internal/catalog
```

The Bash and Zsh PTY execution tests also passed five consecutive runs.

## Bash PTY

The test started Bash with a disposable home and one test alias named `ok` whose command was `echo PTY_OK`.

Keys: `Ctrl+G`, `Enter`.

```text
ALIAS_LENS_TEST> ok
PTY_OK
ALIAS_LENS_TEST> printf 'STATUS=%s\n' "$?"
STATUS=0
ALIAS_LENS_TEST> pwd
<WORKTREE>
```

Native history contained `ok` exactly once. Typing a sentinel command and then pressing `Ctrl+G` cleared the buffer; the sentinel file did not exist and the cancellation assertion returned status 0.

## Zsh PTY

The same disposable test ran in Homebrew Zsh 5.9.2.

Keys: `Ctrl+G`, `Enter`.

```text
ALIAS_LENS_TEST> ok
PTY_OK
ALIAS_LENS_TEST> printf 'STATUS=%s\n' "$?"
STATUS=0
ALIAS_LENS_TEST> pwd
<WORKTREE>
```

Native history contained `ok` exactly once. The non-empty-buffer cancellation assertion returned status 0 and created no sentinel file.

## Login-shell check before restart

The candidate binary was installed in `<DISPOSABLE_HOME>/.local/bin`, and `setup --repair bash` created the isolated integration. A login shell then reported:

```text
<DISPOSABLE_HOME>/.local/bin/alias-lens
function
OK   binary
OK   binary on PATH
OK   .bash_aliases
OK   alias syntax
OK   Bash loading
OK   shell actions
```

`al doctor` also reported three unrelated optional setup items: sync repository, automatic sync, and GitHub CLI.

## Restart check

The first restart attempt used `/tmp/al-phase3/wsl-home`. WSL removed that directory during shutdown. The next shell therefore reported that `al` was not found. This result tests `/tmp` persistence, not Alias Lens startup behavior.

The replacement test home is `<LINUX_HOME>/.cache/alias-lens-phase3-wsl-home`. Before shutdown, its login shell reported:

```text
<DISPOSABLE_HOME>/.local/bin/alias-lens
function
OK   binary
OK   binary on PATH
OK   .bash_aliases
OK   alias syntax
OK   Bash loading
OK   shell actions
DOCTOR_STATUS=0
```

After `wsl --shutdown`, PowerShell started `Ubuntu-24.04` with the persistent disposable home. The login shell reported:

```text
<DISPOSABLE_HOME>/.local/bin/alias-lens
al is a function
OK   binary
OK   binary on PATH
OK   .bash_aliases
OK   alias syntax
OK   Bash loading
OK   shell actions
OK   Git
OK   GitHub SSH
Alias Lens: 3 checks need attention
DOCTOR_STATUS=0
```

The three notices concern an unconfigured sync repository, disabled automatic sync, and a missing optional GitHub CLI. They do not concern the executable or the Bash integration. The restart check passed.
