# Third-party notices

Alias Lens includes an optional local browser bundle built from the packages below. Exact versions and integrity hashes are pinned in `cmd/alias-lens/web-src/package-lock.json`. The complete copyright, permission, and notice text distributed with every production dependency is reproduced in `THIRD_PARTY_LICENSES.txt` and included in release archives.

Apache License 2.0:

- `@pierre/diffs` 1.4.3
- `@pierre/theme` 2.0.0
- `@pierre/theming` 1.0.1

These packages come from the [Pierre repository](https://github.com/pierrecomputer/pierre).

BSD 3-Clause:

- `diff` 9.0.0

ISC:

- `@ungap/structured-clone` 1.4.0

MIT:

- `@shikijs/core`, `@shikijs/engine-javascript`, `@shikijs/engine-oniguruma`, `@shikijs/langs`, `@shikijs/primitive`, `@shikijs/themes`, `@shikijs/transformers`, and `@shikijs/types` 4.4.3
- `shiki` 4.4.3 and `@shikijs/vscode-textmate` 10.0.2
- `@types/hast` 3.0.5, `@types/mdast` 4.0.4, and `@types/unist` 3.0.3
- `ccount` 2.0.1, `character-entities-html4` 2.1.0, `character-entities-legacy` 3.0.0, `comma-separated-tokens` 2.0.3, `dequal` 2.0.3, and `devlop` 1.1.0
- `hast-util-to-html` 9.0.5, `hast-util-whitespace` 3.0.0, `html-void-elements` 3.0.0, and `lru_map` 0.4.1
- `mdast-util-to-hast` 13.2.1; `micromark-util-character` 2.1.1; `micromark-util-encode`, `micromark-util-sanitize-uri`, and `micromark-util-symbol` 2.0.1; and `micromark-util-types` 2.0.2
- `oniguruma-parser` 0.12.2, `oniguruma-to-es` 4.3.6, `property-information` 7.2.0, `regex-recursion` 6.0.2, `regex-utilities` 2.3.0, and `regex` 6.1.0
- `space-separated-tokens` 2.0.2, `stringify-entities` 4.0.4, and `trim-lines` 3.0.1
- `unist-util-is` 6.0.1, `unist-util-position` 5.0.0, `unist-util-stringify-position` 4.0.0, `unist-util-visit` 5.1.0, and `unist-util-visit-parents` 6.0.2
- `vfile` 6.0.3, `vfile-message` 4.0.3, and `zwitch` 2.0.4
